{
  "name": "ai-stories-greece",
  "private": true,
  "type": "module",
  "scripts": {
    "vendor": "node scripts/bundle-vendor.mjs",
    "dev": "npm run vendor && observable preview",
    "build": "npm run vendor && observable build",
    "deploy": "npm run vendor && observable deploy",
    "clean": "rm -rf dist src/.observablehq/cache src/vendor"
  },
  "dependencies": {
    "@observablehq/framework": "^1",
    "d3-dsv": "^3"
  },
  "devDependencies": {
    "@observablehq/plot": "^0.6.17",
    "d3": "^7.9.0",
    "d3-cloud": "^1.2.9",
    "d3-sankey": "^0.12.3",
    "esbuild": "^0.28.2"
  }
}
